name: test_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  expandable_text: ^2.3.0
  marquee: ^2.2.3
  google_fonts: ^5.1.0
  video_player: ^2.7.0
  flutter_svg: ^2.0.7
  lottie: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
